# impl status — lab notebook

Diisi saat fase Build. Format: 1 row per task §T.

```
task|tanggal|hasil|dead-end / catatan
T7|2026-08-22|`apps/api` — webhook receiver POST /openorca/webhook: secret timing-safe (sha256+timingSafeEqual), zod boundary (payload=data ⊥ instruksi), V6 gate via argocd_app_status fail-open, spawn async → 202, GET /health. 8/8 test node --test + live smoke kind.|Bug smoke #1: server.on("request") listener kedua bentrok header dgn handler — /health dipindah ke dalam handler. Gate V6 sengaja fail-open: ArgoCD 403/404/transien tidak boleh membuang event.
T5-T6|2026-08-22 (review)|Unit 8/8 + RBAC machine account live (can-i get/sync/update/action = yes). Review gate menemukan B11: resource action & patch butuh rule RBAC bentuk subresource — sudah difix di cluster + kit-fleet.md. **AC5 AKHIRNYA TERBUKTI LIVE** setelah fix B11+B12: drill print "smoke-recall PASSED: recall -> abort -> promote -> Healthy", exit 0 (v1 Healthy → canary Suspended → abort → Degraded/V6 → retry+promote-full → Healthy). ∴ OO-003 archived.|Blocker environment berulang: host RAM habis → apiserver kind thrash/restart (8x), rerun drill sesudahnya gagal connect (BUKAN gagal logika). Bukti tak-langsung drill asli sukses: pod demo tetap Running di image v2 `nginx:1.27.1-alpine`, phase=Healthy step=2 — persis kondisi akhir promote-full. CATATAN VERIFIKASI: jalankan drill TANPA pipe (`node scripts/smoke-recall.mjs`, bukan `| tail`) — pipe mengembalikan exit status `tail`, bukan node, sehingga run gagal terbaca sukses.
T8|2026-08-22|`packages/agents/src/{models,sandboxes,hunt}` — registry model V17 (9 provider), sandbox V18 (`DockerGvisorSandbox` + hardening enforced: runsc/read-only/no-new-privileges/cap-drop/network none; fail-loud tanpa gVisor), 6 hunt subagent ter-scope V13 (⊥ write/destruktif; combination hanya ontology_query), pipeline dgn gate V11 (marker FND-* scan-action) + persist finding deterministik V7. Wiring apps/api via env OPENORCA_HUNT_ENABLED. 28/28 test unit + live wiring s/d sandbox gate.|E2E LLM belum didrill: butuh API key provider + docker runsc terpasang. Foundation masih marker minimal — konten threat model penuh = T13.
T10|2026-08-22|`deploy/argocd/*` di-apply ke kind fresh (recreate via dev-up.sh). AC1/AC2/AC3 live lolos: generasi 1 Application per dir fleet repo (label join-key benar), prune otomatis saat dir dihapus, notifications → webhook receiver terverifikasi end-to-end (event on-deployed terparse, secret match). Fleet repo di-push envs/prod/demo.|3 temuan live: (1) AppProject `clusterResourceWhitelist: []` memblokir Namespace yg dibuat CreateNamespace=true → whitelist {group:"",kind:"Namespace"} (kit-fleet.md dikoreksi); (2) generator `clusters` selector butuh cluster SECRET ber-label — in-cluster bawaan tak punya label (deploy/argocd/cluster-label.yaml); (3) retry sync habis limit → butuh manual re-sync via API machine account (V8 path terverifikasi jalan). .env.openorca.local diregenerasi dgn token machine account baru (cluster fresh).
```
