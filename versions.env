# Pinned versions — OpenOrca dev stack.
# Usage: docker compose --env-file versions.env up -d
TYPEDB_VERSION=3.12.3
POSTGRES_VERSION=18.4-alpine
# Cluster components (kind / ArgoCD / Argo Rollouts) are pinned by the OO-001
# install scripts, not by compose — see factory/specs/inbox/OO-001-dev-infra.md.
